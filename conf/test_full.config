/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Full-size test profile (cohort scale)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Re-uses the smoke samplesheet by default; override with --input <path>
    when running a real cohort.

    Heavy DBs are external (sylph_db, busco_downloads) — override at runtime
    if the Heimdal default paths don't apply.
----------------------------------------------------------------------------------------
*/

params {
    config_profile_name        = 'Full-size test profile'
    config_profile_description = 'Full cohort run — defaults to the smoke samplesheet; override --input for a real cohort'

    input = "${projectDir}/assets/samplesheet.csv"

    refs_dir        = "${projectDir}/resources/references"
    ref_manifest    = "${projectDir}/resources/references/ref_candida_info.tsv"
    amr_panel       = "${projectDir}/assets/amr_db/epicandi_AMR_panel_unique.tsv"
    rosetta         = "${projectDir}/assets/amr_db/candida_rosetta_v2.tsv"

    sylph_db        = '/home/asanzc/epicandi_databases/sylph/sketches/epicandi.syldb'
    busco_downloads = '/home/asanzc/epicandi/resources/busco_downloads'
}
